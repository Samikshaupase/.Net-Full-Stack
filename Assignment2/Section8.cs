using System;

namespace Assignment2
{
    public class Section8
    {
        public static void Run()
        {
            Console.WriteLine("\n--- Section 8: Logical & Comparison Operators ---");

            int a = 10, b = 20;
            Console.WriteLine(a < b && b > 15);
            Console.WriteLine(a == b || a < b);
            //int x=10;
            //bool isReady = true;
            //bool hasData = false;
            //if (x > 0 && x < 100) { /* in range */ }
            //if (!(isReady || hasData)) return;
        }
    }
}
